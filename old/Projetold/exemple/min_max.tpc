entier min(a, b){
	if(a < b)
		return a;
	return b;
}

entier max(a, b){
	if(a > b)
		return a;
	return b;
}

main(){
	var a, b, c;
	a = 10;
	b = 20;
	c = min(a, b);
	print(c);
	c = max(a, b);
	print(c);
}